version: '3.8'

services:
  app:
    image: debian:trixie-backports
    container_name: app
    ports:
      - "3000:3000"
    volumes:
      - /var/run/tappd.sock:/var/run/tappd.sock
    stdin_open: true
    tty: true
    command: tail -f /dev/null


["bash", "-c", "apt-get update && apt-get install -y ttyd && ttyd -p 7681 -i 0.0.0.0 --writable bash"]
    stdin_open: true
    tty: true
