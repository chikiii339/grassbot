version: '3.8'

services:
  web-terminal:
    image: debian:trixie-backports
    container_name: web-terminal
    ports:
      - "7681:7681"
    command: ["bash", "-c", "apt-get update && apt-get install -y ttyd && ttyd -p 7681 -i 0.0.0.0 --writable bash"]
    stdin_open: true
    tty: true
